# CatBlock 项目开发规则

## UI 与节点

- 静态 UI 的层级、尺寸、位置、颜色和文字样式必须优先在 Cocos Creator 场景或预制件中配置。
- 尽量避免在业务脚本中创建静态节点；脚本主要负责查找预制件节点、绑定数据、处理状态和交互。
- 只有数量或结构由运行时数据决定的对象（例如待选方块、拖拽预览、消除特效）才允许由代码动态创建节点。
- 调整棋盘与底部槽位时，应统一使用同一套设计尺寸和缩放基准，避免局部节点独立缩放造成尺寸不协调。

## 美术与生图

- 项目唯一主美术参考图为 `docs/art/catblock-main-art-reference.png`，详细规范为 `docs/ART_STYLE.md`。
- 新增或修改 UI、纸箱、猫咪、图标、特效和宣传素材时，必须遵循主参考图的城市快递主题、彩色纸箱材质、贴纸式轮廓、核心色板和视觉层级。
- 凡是使用生图工具生成项目图片，必须将主美术参考图作为主要视觉参考输入，并在提示词中明确引用 `docs/ART_STYLE.md` 的风格、材质、配色和禁止项。
- 生图结果在接入项目前必须对照 `docs/ART_STYLE.md` 的验收清单检查；不符合纸箱占位、整箱着色、猫咪出箱、技能贴纸位置或底部货箱规则的图片不得直接使用。
- 生成图中的伪文字、乱码、商标和水印不得进入正式资源；正式 UI 文案应由 Cocos Label 或项目字体资源实现。

## 代码规范

- 新增或修改的业务代码应为关键类、字段、方法及非直观逻辑补充简洁的中文注释。
- 注释说明设计意图、约束或原因，不重复代码本身已经清楚表达的内容。
- UI 脚本与玩法逻辑保持分离；预制件负责表现结构，组件负责数据和交互。
- 项目代码不得主动使用 `throw` 抛出异常；遇到非法状态时应记录中文日志，并通过安全返回、可空结果或明确的降级方案处理。

## Git 规范

- Git 提交信息使用中文，准确说明本次变更的目标。
- 提交前至少执行类型检查、相关测试和 `git diff --check`。
- 只提交当前任务涉及的文件，不得顺带提交编辑器缓存、临时目录或用户的无关改动。
